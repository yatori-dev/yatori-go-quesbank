package api_server

import (
	"github.com/gin-gonic/gin"
)

func (LocalQuestionApi) BankQueryQuestionApi(c *gin.Context) {
	//pageNum := c.Param("pageNum")
	//pageSize := c.Param("pageSize")
	//questionbank.EsQueryAll()
}
